VIEW "EXPERT_WITH_TEAM"( 
	"NAME",
	"ID",
	"TEAM_ID",
	"REPORTS_TO",
	"SKILL_SET",
	"EXT_ID" ) 
AS SELECT
	 X.NAME,
	 X.ID,
	 M.TEAM_ID,
	 X.REPORTS_TO,
	 X.SKILL_SET,
	 X.EXT_ID
FROM ( EXPERT AS X 
	LEFT OUTER JOIN TEAM_MEMBER AS M ON ( X.ID = M.EXPERT_ID ) ) WITH READ ONLY