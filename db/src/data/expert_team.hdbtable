COLUMN TABLE "EXPERT_TEAM" (
	"ID" INTEGER NOT NULL,
	"TEAM_NAME" NVARCHAR(200),
	"DESCRIPTION" NVARCHAR(5000),
	"TEAM_LEAD" INTEGER,
	"PARENT_TEAM" INTEGER,
	"SOURCE" NVARCHAR(10),
	PRIMARY KEY ("ID")
)
WITH ASSOCIATIONS( JOIN EXPERT_WITH_TEAM AS EXPERTS ON ID = EXPERTS.TEAM_ID )